ANTHROPIC_API_KEY=sk-ant-...
# Shared secret that gates /api/chat (and therefore your Anthropic spend).
# Required in production; generate one with: openssl rand -base64 32
# Unset in development = open, but only for requests to localhost.
EASYMODE_ACCESS_TOKEN=
