## What & why

<!-- One or two sentences. Link the spec/plan section if this implements one. -->

## Checklist

- [ ] `npm run lint`, `npm run typecheck`, `npm test`, `npm run build` all pass locally
- [ ] Cost math untouched, or `lib/__tests__/costs.test.ts` updated to match
- [ ] Routing behavior untouched, or `npm run eval` re-run (paste the score)
- [ ] Model IDs/prices only ever change in `lib/pricing.ts` (single source of truth)
